Refer Kubernetes documentation/cheat sheet for more details

kubectl run <pod_name> --image=nginx --dry-run=client -o yaml > ngpod.yaml                     // give a pod manifest file ngpod.yaml 
kubectl create deployment <name> --image=nginx --dry-run=client -o yaml > ngdep.yaml           // gives depoyment manifest file


VIEWING AND LOCATING RESOURSES

kubectl get services                # List all services in the namespace
kubectl get pods --all-namespaces   # List all Pods in all namespaces
kubectl get pods -o wide            # List all Pods in the current namespace, with more details
kubectl get deployment my-dep       # List a specific deployment
kubectl get pods                    # List all Pods in the namespace
kubectl get pod my-pod -o yaml      # Get the YAML of a Pod

UPDATING RESOURSE
------------------
kubectl set image deployment/frontend www=image:v2           # Rollout the "www" containers of the "frontend" deployment, updating the image
kubectl rollout history deployment/frontend                  # Check the deployment history, including the revision
kubectl rollout undo deployment/frontend                     # Rollback to the previous deployment
kubectl rollout undo deployment/frontend --to-revision=2     # Rollback to a specific revision
kubectl rollout status -w deployment/frontend                # Track the status of the "frontend" upgrade until it is completed without interruption
kubectl rollout restart deployment/frontend                  # Continuously restart the "frontend" deployment
