To enable password authentication other than key.pem login

IN LINUX MACHINE
$ adduser <username>  ,create password
$ vim /etc/ssh/sshd_config        > enable password auth
$ vim /etc/ssh/sshd_config.d/60-cloudimg-settings.conf     > enable password auth
$ systemctl restart ssh 

login >   $ ssh <username>@ip    
