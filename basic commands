create a new repository on the command line
-------------------------------------------

echo "# <reponame>" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/akhileehh/<reponame>.git
git push -u origin main


push an existing repository from the command line
-------------------------------------------------

git remote add origin https://github.com/akhileehh/<reponame>.git
git branch -M main                       >>> create branch named main
git push -u origin main                  >>> push all content from local to remmote repo named main


git log --oneline                         >>> show online commit id
git show <commit id>                      >>> view changes
git pull                                  >>> pull leatest repo 
git brach -c <name>                       >>> create branch
git brach -a                              >>> list all braches
git checkout <brachname> | git switch     >>> change branch
git rm <file name>                        >>> to remove files
vim gitignore                             >>> add file names that as to ignored by git , *.log all files with .log ending will be ignored
